name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    uses: nichu42/shared-workflows/.github/workflows/release-extension.yml@main
    secrets: inherit
