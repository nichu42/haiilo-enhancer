name: Release

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  release:
    uses: nichu42/shared-workflows/.github/workflows/release-extension.yml@main
    secrets: inherit
